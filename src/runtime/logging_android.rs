// use log::LevelFilter;

// use android_logger::Config;

pub fn init() {
    // We use our own logger, so dont initialize this one
    // android_logger::init_once(Config::default().with_max_level(LevelFilter::Debug));
}
